#ifndef MAIN_H
#define MAIN_H
/*
struct distanceYawRate_t {
	double distance;
	double yawRate;
};

/*#include <atomic>

#define HW_REGS_BASE ( ALT_STM_OFST )
#define HW_REGS_SPAN ( 0x04000000 )
#define HW_REGS_MASK ( HW_REGS_SPAN - 1 )

//const int CAR_LED_HEADLIGHT_SHIFT = 1;
/*const int CAR_LED_LED0_SHIFT = 3;
const int CAR_LED_LED1_SHIFT = 4;
const int CAR_LED_ERR_SHIFT = 5;

const int MOTOR_CONTROL_EN_SHIFT = 0;
const int MOTOR_CONTROL_INHIBIT_SHIFT = 1;

extern volatile unsigned long* MotorControlAddress;
extern volatile unsigned long* CarSensorsAddress;
extern volatile unsigned long* CarLedsAddress;
extern volatile unsigned long* LedsAddress;
extern volatile bool Stop;
extern volatile std::atomic<double> MaxSpeed;*/

#endif
